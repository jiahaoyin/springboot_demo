<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.SysRoleMapper">

    <select id="selectRolesByUserId" parameterType="Long" resultType="com.example.demo.entity.SysRole">
        select distinct r.*
        from sys_role r
        left join sys_user_role ur on ur.role_id = r.role_id
        left join sys_user u on u.user_id = ur.user_id
        where r.del_flag = '0' and r.status = '0'
        and ur.user_id = #{userId}
    </select>

    <select id="selectRolePermissionByUserId" parameterType="Long" resultType="String">
        select distinct r.role_key
        from sys_role r
        left join sys_user_role ur on ur.role_id = r.role_id
        left join sys_user u on u.user_id = ur.user_id
        where r.del_flag = '0' and r.status = '0'
        and ur.user_id = #{userId}
    </select>

    <delete id="deleteRoleMenuByRoleIds">
        delete from sys_role_menu where role_id in
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </delete>

    <delete id="deleteRoleDeptByRoleIds">
        delete from sys_role_dept where role_id in
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </delete>

    <insert id="insertRoleMenu">
        insert into sys_role_menu(role_id, menu_id) values
        <foreach collection="menuIds" item="menuId" separator=",">
            (#{roleId}, #{menuId})
        </foreach>
    </insert>

    <insert id="insertRoleDept">
        insert into sys_role_dept(role_id, dept_id) values
        <foreach collection="deptIds" item="deptId" separator=",">
            (#{roleId}, #{deptId})
        </foreach>
    </insert>
</mapper> 